image: nixos/nix:latest

variables:
  GIT_SUBMODULE_STRATEGY: none
  NIX_CONFIG: "extra-experimental-features = nix-command flakes"
  http_proxy: $CODE_PROXY
  https_proxy: $CODE_PROXY
  no_proxy: $CODE_NO_PROXY,$CI_REGISTRY,127.0.0.1,captain.rtf.siemens.net,.code.siemens.io,code.siemens.io

pages:
  script:
    - nix build .#quartz-gitlab --override-input eis-notes "git+https://gitlab-ci-token:${CI_JOB_TOKEN}@code.siemens.com/eis/eis-notes.git"
    - cp -r result/public public
  artifacts:
    paths: [public]
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
